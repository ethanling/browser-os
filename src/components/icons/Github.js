import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'
import { faGithubSquare } from '@fortawesome/free-brands-svg-icons'

const Github = ({ size }) => (
    <FontAwesomeIcon icon={faGithubSquare} size={`${size}x`} />
)

export default Github;
